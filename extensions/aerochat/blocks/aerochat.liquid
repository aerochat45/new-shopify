{% comment %}
  AeroChat Widget - API Loader Approach
  This block uses JavaScript to fetch the script URL from AeroChat API.
  Includes localStorage caching for performance.
  
  For a faster, metafield-based approach, use the "AeroChat Widget (Metafield)" block instead.
{% endcomment %}

{% schema %}
{
  "name": "AeroChat Widget (API Loader)",
  "target": "body",
  "javascript": "chatbox-loader.js",
  "settings": [
    {
      "type": "paragraph",
      "content": "This block loads the AeroChat widget by making an API call. Uses localStorage caching to improve performance."
    }
  ]
}
{% endschema %}
